Se necesita 4 metodos, datos ingresados por el usuario. Se va a pedir lo que el 
usuario necesite calcular en el metodo principal para despues ir al metodo 
correspondiente para realizar el procedimiento, pedir los datos e imprimir. 
Intervienen metodos, procedimientos, scanner, else if. Se espera mostrar la 
respuesta del area calculada que ingrese el usuario.